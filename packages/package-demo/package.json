{
  "name": "package-demo",
  "version": "0.0.0",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build:lib": "tsc -p ./tsconfig.json",
    "build": "npm run clean && npm run build:lib",
    "lint": "eslint --ext .ts src/ __tests__/"
  },
  "license": "MIT"
}
